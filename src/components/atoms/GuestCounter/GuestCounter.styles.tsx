import { makeStyles } from "@mui/styles";

const GuestCounterStyles = makeStyles(() => ({
    container: {
        display: 'flex',
        alignItems: 'center',
        marginRight: '-10px'
    },
    label: {
        color: "#FFFFFF",
        fontSize: "18px",
        fontWeight: "600",
    }
}));

export default GuestCounterStyles;