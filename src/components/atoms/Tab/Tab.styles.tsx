import { makeStyles } from "@mui/styles";

const TabStyles = makeStyles(() => ({
    tab: {
        fontWeight: "300",
        fontSize: "18px",
        textDecoration: "none",
        color: "#FFFFFF",
        marginRight: "20px",
        // zIndex:"1000"
    }
}));

export default TabStyles;