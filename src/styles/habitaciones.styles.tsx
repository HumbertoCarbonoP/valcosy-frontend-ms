import { makeStyles } from "@mui/styles";

const HabitacionesStyles = makeStyles(() => ({
    habitacionesContainer: {
        backgroundColor: "rgba(0, 0, 0, 0.5)"
    }
}));

export default HabitacionesStyles;