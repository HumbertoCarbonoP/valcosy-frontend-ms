import { makeStyles } from "@mui/styles";

const BookingStyles = makeStyles(() => ({
    mainContainer: {
        marginTop: '150px',
        marginBottom: '50px'
    },
}));

export default BookingStyles;